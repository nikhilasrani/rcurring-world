---
phase: 03-game-systems
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/types.ts
  - src/utils/constants.ts
  - src/data/items/items.json
  - src/data/pickups/mg-road-pickups.json
  - src/data/journal/mg-road-discoveries.json
  - src/systems/QuestManager.ts
  - src/systems/InventoryManager.ts
  - src/systems/JournalManager.ts
  - src/systems/SaveManager.ts
  - tests/unit/quest-manager.test.ts
  - tests/unit/inventory-manager.test.ts
  - tests/unit/journal-manager.test.ts
  - tests/unit/save-manager.test.ts
autonomous: true
requirements:
  - QUST-01
  - QUST-02
  - QUST-04
  - INVT-01
  - INVT-03
  - JRNL-01
  - JRNL-02
  - JRNL-03
  - SAVE-01
  - SAVE-02
  - SAVE-03
  - SAVE-04

must_haves:
  truths:
    - "GameState interface captures all saveable state (player, quests, inventory, discovery, settings)"
    - "QuestManager can accept/track/complete quests with one-active-at-a-time constraint"
    - "InventoryManager can add/check items with 12-slot capacity and no duplicates"
    - "JournalManager computes zone completion percentage from discovery data"
    - "SaveManager serializes/deserializes GameState to/from localStorage"
    - "All managers are pure TypeScript with no Phaser imports"
    - "8 Bengaluru-specific items exist with culturally authentic flavor text"
  artifacts:
    - path: "src/utils/types.ts"
      provides: "GameState, QuestStatus, QuestState, InventoryItem, QuestDef, QuestObjective, ItemDef, PickupDef interfaces"
      contains: "export interface GameState"
    - path: "src/utils/constants.ts"
      provides: "Phase 3 EVENTS constants"
      contains: "QUEST_OFFERED"
    - path: "src/systems/QuestManager.ts"
      provides: "Quest state machine"
      exports: ["QuestManager"]
    - path: "src/systems/InventoryManager.ts"
      provides: "Item collection logic"
      exports: ["InventoryManager"]
    - path: "src/systems/JournalManager.ts"
      provides: "Discovery tracking aggregation"
      exports: ["JournalManager"]
    - path: "src/systems/SaveManager.ts"
      provides: "localStorage persistence"
      exports: ["SaveManager"]
    - path: "src/data/items/items.json"
      provides: "8 item definitions"
      contains: "filter-coffee"
    - path: "tests/unit/quest-manager.test.ts"
      provides: "Quest system test coverage"
      min_lines: 40
  key_links:
    - from: "src/systems/QuestManager.ts"
      to: "src/utils/types.ts"
      via: "QuestState, QuestStatus types"
      pattern: "import.*QuestState.*from"
    - from: "src/systems/SaveManager.ts"
      to: "src/utils/types.ts"
      via: "GameState interface"
      pattern: "import.*GameState.*from"
    - from: "src/systems/JournalManager.ts"
      to: "src/utils/types.ts"
      via: "DiscoveryState, ItemDef"
      pattern: "import.*from.*types"
---

<objective>
Create the foundational type contracts, data files, and pure-logic system managers for Phase 3 game systems, plus comprehensive unit tests.

Purpose: Establish the data model and testable business logic that all subsequent plans depend on. Every UI component, scene integration, and save/load flow references these types and managers.
Output: 4 new system managers (QuestManager, InventoryManager, JournalManager, SaveManager), extended type definitions, Phase 3 event constants, and 3 JSON data files (items catalog, world pickups, journal discoveries).
</objective>

<execution_context>
@/home/nikhil/Code/Game/rcurring-world/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikhil/Code/Game/rcurring-world/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-game-systems/03-CONTEXT.md
@.planning/phases/03-game-systems/03-RESEARCH.md

<interfaces>
<!-- Existing types to extend -->
From src/utils/types.ts:
```typescript
export interface PlayerState {
  name: string;
  gender: 'male' | 'female';
  position: { x: number; y: number };
  facing: Direction;
  isRunning: boolean;
}

export interface DiscoveryState {
  discoveredZones: string[];
  discoveredLandmarks: string[];
  currentZone: string | null;
  currentInterior: string | null;
}

export interface InteractionTarget {
  type: 'npc' | 'sign' | 'door';
  id: string;
  position: { x: number; y: number };
}

export interface NPCDef { ... }
export interface InteriorDef { ... }
```

From src/utils/constants.ts:
```typescript
export const EVENTS = {
  PLAYER_MOVE: 'player-move',
  DIALOGUE_OPEN: 'dialogue-open',
  DIALOGUE_CLOSE: 'dialogue-close',
  DIALOGUE_ADVANCE: 'dialogue-advance',
  NPC_INTERACT: 'npc-interact',
  SIGN_INTERACT: 'sign-interact',
  ZONE_ENTER: 'zone-enter',
  MOVEMENT_FREEZE: 'movement-freeze',
  BUILDING_ENTER: 'building-enter',
  BUILDING_EXIT: 'building-exit',
  // ... other Phase 1-2 events
} as const;

export const ASSETS = { ... } as const;
```

From src/data/zones/mg-road.json:
```json
{
  "landmarks": [
    { "id": "chinnaswamy-stadium", "name": "Chinnaswamy Stadium" },
    { "id": "ub-city", "name": "UB City" },
    { "id": "cubbon-park", "name": "Cubbon Park" },
    { "id": "vidhana-soudha", "name": "Vidhana Soudha" },
    { "id": "mg-road-metro", "name": "MG Road Metro Station" }
  ]
}
```

Existing NPC IDs (from src/data/npcs/*.json): npc-chai-walla, npc-auto-driver, npc-jogger, npc-shopkeeper, npc-guard
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type contracts, constants, data files</name>
  <files>
    src/utils/types.ts,
    src/utils/constants.ts,
    src/data/items/items.json,
    src/data/pickups/mg-road-pickups.json,
    src/data/journal/mg-road-discoveries.json
  </files>
  <read_first>
    src/utils/types.ts,
    src/utils/constants.ts,
    src/data/zones/mg-road.json,
    src/data/npcs/chai-walla.json,
    src/data/npcs/auto-driver.json,
    src/data/npcs/jogger.json,
    src/data/npcs/shopkeeper.json,
    src/data/npcs/guard.json
  </read_first>
  <behavior>
    - items.json has 8 items: filter-coffee, masala-dosa, jasmine-flowers, metro-token, park-leaf, shopping-bag, old-bengaluru-photo, best-filter-coffee
    - Each item has id, name, description, iconKey fields
    - mg-road-pickups.json has 5 entries with id, itemId, position {x,y}, zone fields
    - mg-road-discoveries.json has places (5 landmarks), npcs (6 incl. park-coffee-vendor), items (8 items) with totals
    - GameState interface has version, timestamp, player, quests, inventory, discovery, settings fields
    - QuestStatus is 'not-started' | 'offered' | 'accepted' | 'in-progress' | 'complete'
    - InteractionTarget.type union includes 'pickup' and 'metro-map'
  </behavior>
  <action>
    EXTEND src/utils/types.ts with the following NEW interfaces (keep all existing interfaces unchanged):

    ```typescript
    export type QuestStatus = 'not-started' | 'offered' | 'accepted' | 'in-progress' | 'complete';

    export interface QuestState {
      id: string;
      status: QuestStatus;
      objectivesCompleted: string[];
      objectivesTotal: number;
    }

    export interface InventoryItem {
      id: string;
      name: string;
      description: string;
      iconKey: string;
      source: 'quest-reward' | 'npc-gift' | 'world-pickup';
    }

    export interface QuestDef {
      id: string;
      name: string;
      description: string;
      giverNpcId: string;
      objectives: QuestObjective[];
      reward: {
        itemId: string;
        dialogueUnlock?: string;
      };
    }

    export interface QuestObjective {
      id: string;
      description: string;
      type: 'interact-npc' | 'visit-location' | 'collect-item';
      targetId: string;
    }

    export interface ItemDef {
      id: string;
      name: string;
      description: string;
      iconKey: string;
    }

    export interface PickupDef {
      id: string;
      itemId: string;
      position: { x: number; y: number };
      zone: string;
    }

    export interface JournalDiscoveries {
      zone: string;
      places: { id: string; name: string }[];
      npcs: { id: string; name: string }[];
      items: { id: string; name: string }[];
    }

    export interface GameState {
      version: number;
      timestamp: number;
      player: {
        name: string;
        gender: 'male' | 'female';
        position: { x: number; y: number };
        facing: string;
        isRunning: boolean;
        currentZone: string;
        isInInterior: boolean;
        interiorId?: string;
      };
      quests: Record<string, QuestState>;
      inventory: InventoryItem[];
      discovery: {
        zones: string[];
        landmarks: string[];
        npcsMetIds: string[];
        collectedPickupIds: string[];
      };
      settings: {
        musicVolume: number;
        sfxVolume: number;
        runDefault: boolean;
      };
    }

    /** Interior interactable definition (for coffee counter, metro map wall, etc.) */
    export interface InteriorInteractable {
      id: string;
      type: 'counter' | 'metro-map' | 'object';
      position: { x: number; y: number };
      dialogue?: DialogueData;
    }
    ```

    UPDATE the InteractionTarget type union to include 'pickup' and 'metro-map':
    ```typescript
    export interface InteractionTarget {
      type: 'npc' | 'sign' | 'door' | 'pickup' | 'metro-map';
      id: string;
      position: { x: number; y: number };
    }
    ```

    EXTEND src/utils/constants.ts EVENTS object with these new keys (append, do NOT modify existing):
    ```typescript
    // Quest events
    QUEST_OFFERED: 'quest-offered',
    QUEST_ACCEPTED: 'quest-accepted',
    QUEST_OBJECTIVE_COMPLETE: 'quest-objective-complete',
    QUEST_COMPLETE: 'quest-complete',
    QUEST_DECLINED: 'quest-declined',
    // Inventory events
    ITEM_COLLECTED: 'item-collected',
    ITEM_PICKUP_INTERACT: 'item-pickup-interact',
    // Journal events
    NPC_MET: 'npc-met',
    // Menu events
    PAUSE_MENU_OPEN: 'pause-menu-open',
    PAUSE_MENU_CLOSE: 'pause-menu-close',
    // Save events
    GAME_SAVED: 'game-saved',
    GAME_LOADED: 'game-loaded',
    SAVE_ICON_SHOW: 'save-icon-show',
    // Dialogue choice
    DIALOGUE_CHOICE: 'dialogue-choice',
    // Metro
    METRO_MAP_OPEN: 'metro-map-open',
    METRO_TRAVEL_START: 'metro-travel-start',
    ```

    EXTEND ASSETS object with:
    ```typescript
    SPRITE_ITEM_ICONS: 'item-icons',
    SPRITE_SPARKLE: 'sparkle',
    SPRITE_SAVE_ICON: 'save-icon',
    SPRITE_HAMBURGER: 'hamburger-icon',
    SPRITE_NPC_PARK_COFFEE: 'npc-park-coffee-vendor',
    ```

    CREATE src/data/items/items.json with 8 items:
    ```json
    [
      { "id": "filter-coffee", "name": "Filter Coffee", "description": "Steel tumbler, strong decoction", "iconKey": "item-filter-coffee" },
      { "id": "masala-dosa", "name": "Masala Dosa", "description": "Crispy, with coconut chutney", "iconKey": "item-masala-dosa" },
      { "id": "jasmine-flowers", "name": "Jasmine Flowers", "description": "Malli hoo for the hair", "iconKey": "item-jasmine-flowers" },
      { "id": "metro-token", "name": "Namma Metro Token", "description": "Namma Metro Purple Line", "iconKey": "item-metro-token" },
      { "id": "park-leaf", "name": "Cubbon Park Leaf", "description": "From a 100-year rain tree", "iconKey": "item-park-leaf" },
      { "id": "shopping-bag", "name": "UB City Shopping Bag", "description": "UB City premium retail", "iconKey": "item-shopping-bag" },
      { "id": "old-bengaluru-photo", "name": "Old Bengaluru Photo", "description": "A faded glimpse of the garden city", "iconKey": "item-old-photo" },
      { "id": "best-filter-coffee", "name": "Best Filter Coffee", "description": "The pride of MG Road", "iconKey": "item-best-coffee" }
    ]
    ```

    CREATE src/data/pickups/mg-road-pickups.json with 5 world pickups:
    ```json
    [
      { "id": "pickup-jasmine", "itemId": "jasmine-flowers", "position": { "x": 10, "y": 15 }, "zone": "mg-road" },
      { "id": "pickup-metro-token", "itemId": "metro-token", "position": { "x": 9, "y": 7 }, "zone": "mg-road" },
      { "id": "pickup-dosa", "itemId": "masala-dosa", "position": { "x": 38, "y": 35 }, "zone": "mg-road" },
      { "id": "pickup-leaf", "itemId": "park-leaf", "position": { "x": 5, "y": 12 }, "zone": "mg-road" },
      { "id": "pickup-bag", "itemId": "shopping-bag", "position": { "x": 28, "y": 38 }, "zone": "mg-road" }
    ]
    ```
    Note: pickup-metro-token position is inside metro station interior. Adjust positions during integration to match walkable tiles near landmarks.

    CREATE src/data/journal/mg-road-discoveries.json:
    ```json
    {
      "zone": "mg-road",
      "places": [
        { "id": "chinnaswamy-stadium", "name": "Chinnaswamy Stadium" },
        { "id": "ub-city", "name": "UB City" },
        { "id": "cubbon-park", "name": "Cubbon Park" },
        { "id": "vidhana-soudha", "name": "Vidhana Soudha" },
        { "id": "mg-road-metro", "name": "MG Road Metro Station" }
      ],
      "npcs": [
        { "id": "npc-chai-walla", "name": "Raju" },
        { "id": "npc-auto-driver", "name": "Auto Driver" },
        { "id": "npc-jogger", "name": "Jogger" },
        { "id": "npc-shopkeeper", "name": "Shopkeeper" },
        { "id": "npc-guard", "name": "Guard" },
        { "id": "npc-park-coffee-vendor", "name": "Park Coffee Vendor" }
      ],
      "items": [
        { "id": "filter-coffee", "name": "Filter Coffee" },
        { "id": "masala-dosa", "name": "Masala Dosa" },
        { "id": "jasmine-flowers", "name": "Jasmine Flowers" },
        { "id": "metro-token", "name": "Namma Metro Token" },
        { "id": "park-leaf", "name": "Cubbon Park Leaf" },
        { "id": "shopping-bag", "name": "UB City Shopping Bag" },
        { "id": "old-bengaluru-photo", "name": "Old Bengaluru Photo" },
        { "id": "best-filter-coffee", "name": "Best Filter Coffee" }
      ]
    }
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/unit/constants.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/types.ts contains `export interface GameState`
    - src/utils/types.ts contains `export type QuestStatus = 'not-started'`
    - src/utils/types.ts contains `export interface InventoryItem`
    - src/utils/types.ts contains `export interface QuestDef`
    - src/utils/types.ts contains `export interface ItemDef`
    - src/utils/types.ts contains `export interface PickupDef`
    - src/utils/types.ts contains `export interface JournalDiscoveries`
    - src/utils/types.ts contains `export interface InteriorInteractable`
    - src/utils/types.ts contains `type: 'npc' | 'sign' | 'door' | 'pickup' | 'metro-map'`
    - src/utils/constants.ts contains `QUEST_OFFERED: 'quest-offered'`
    - src/utils/constants.ts contains `ITEM_COLLECTED: 'item-collected'`
    - src/utils/constants.ts contains `PAUSE_MENU_OPEN: 'pause-menu-open'`
    - src/utils/constants.ts contains `DIALOGUE_CHOICE: 'dialogue-choice'`
    - src/utils/constants.ts contains `METRO_MAP_OPEN: 'metro-map-open'`
    - src/utils/constants.ts contains `SPRITE_ITEM_ICONS: 'item-icons'`
    - src/utils/constants.ts contains `SPRITE_SPARKLE: 'sparkle'`
    - src/data/items/items.json contains `filter-coffee`
    - src/data/items/items.json contains `best-filter-coffee`
    - src/data/items/items.json contains 8 item entries
    - src/data/pickups/mg-road-pickups.json contains 5 pickup entries
    - src/data/journal/mg-road-discoveries.json contains `chinnaswamy-stadium`
    - src/data/journal/mg-road-discoveries.json contains `npc-park-coffee-vendor`
    - Existing EVENTS keys (PLAYER_MOVE, DIALOGUE_OPEN, etc.) remain unchanged
    - Existing type interfaces (PlayerState, NPCDef, etc.) remain unchanged
  </acceptance_criteria>
  <done>All Phase 3 types exported from types.ts, all Phase 3 events in constants.ts, all 3 JSON data files created with correct schema and content</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure-logic system managers with unit tests</name>
  <files>
    src/systems/QuestManager.ts,
    src/systems/InventoryManager.ts,
    src/systems/JournalManager.ts,
    src/systems/SaveManager.ts,
    tests/unit/quest-manager.test.ts,
    tests/unit/inventory-manager.test.ts,
    tests/unit/journal-manager.test.ts,
    tests/unit/save-manager.test.ts
  </files>
  <read_first>
    src/utils/types.ts,
    src/utils/constants.ts,
    src/ui/DialogueController.ts,
    tests/unit/zone-manager.test.ts
  </read_first>
  <behavior>
    QuestManager:
    - acceptQuest returns false if another quest is already active
    - offerQuest sets status to 'offered' (idempotent)
    - acceptQuest sets status to 'accepted' and assigns activeQuestId
    - completeObjective marks objectives, transitions to 'in-progress', then 'complete' when all done
    - completeObjective on final objective sets status to 'complete' and clears activeQuestId
    - getProgress returns {completed, total} for a quest
    - loadState/getState round-trip preserves quest data
    - declineQuest keeps quest in 'offered' state (re-offerable)

    InventoryManager:
    - addItem succeeds and returns true for first item
    - addItem returns false for duplicate itemId (no duplicates)
    - addItem returns false when at capacity (12 slots)
    - hasItem returns true for collected items, false otherwise
    - getItems returns readonly array
    - loadState/getState round-trip preserves items

    JournalManager:
    - getCompletionPercentage computes Places + NPCs + Items discovered / total
    - getPlacesDiscovered returns only discovered entries
    - getNPCsMet returns only met NPC entries
    - getItemsFound returns only collected item entries
    - getUndiscovered returns the ??? placeholder count

    SaveManager:
    - save serializes GameState to localStorage key 'rcurring-world-save'
    - load deserializes GameState from localStorage
    - load returns null for missing save
    - load returns null for corrupt JSON (no crash)
    - hasSave returns true when save exists, false otherwise
    - deleteSave removes the key
    - save returns false on QuotaExceededError (try/catch)
  </behavior>
  <action>
    CREATE src/systems/QuestManager.ts as a pure TypeScript class (NO Phaser imports):
    - Private Map<string, QuestState> and activeQuestId: string | null
    - Constructor takes optional onStateChange callback: (questId: string, state: QuestState) => void
    - hasActiveQuest(): boolean
    - getActiveQuestId(): string | null
    - getQuestState(questId: string): QuestState | undefined
    - offerQuest(questId: string, totalObjectives: number): void -- creates quest entry with status 'offered' if not exists
    - acceptQuest(questId: string): boolean -- returns false if active quest exists or quest not found/complete. Sets status 'accepted', assigns activeQuestId
    - declineQuest(questId: string): void -- keeps status as 'offered' (no-op essentially, but marks intent)
    - completeObjective(questId: string, objectiveId: string): boolean -- adds to objectivesCompleted, sets 'in-progress', auto-completes when all objectives done (sets 'complete', clears activeQuestId)
    - getProgress(questId: string): { completed: number; total: number } | null
    - loadState(questStates: Record<string, QuestState>): void -- restores from save
    - getState(): Record<string, QuestState> -- snapshot for save
    - Calls onStateChange callback on every state mutation

    CREATE src/systems/InventoryManager.ts as a pure TypeScript class (NO Phaser imports):
    - Private items: InventoryItem[] with capacity = 12
    - addItem(item: InventoryItem): boolean -- false if full or duplicate id
    - hasItem(itemId: string): boolean
    - getItems(): readonly InventoryItem[]
    - getCount(): number
    - loadState(items: InventoryItem[]): void
    - getState(): InventoryItem[]

    CREATE src/systems/JournalManager.ts as a pure TypeScript class (NO Phaser imports):
    - Constructor takes JournalDiscoveries data (the totals checklist from JSON)
    - Methods accept current discovery state (zones, landmarks, npcsMetIds, collectedPickupIds, inventoryItemIds) as parameters -- does NOT store mutable state itself, it computes on demand
    - getCompletionPercentage(discoveredLandmarks: string[], npcsMetIds: string[], inventoryItemIds: string[]): number -- returns 0-100 integer
    - getPlacesDiscovered(discoveredLandmarks: string[]): { discovered: {id,name}[], undiscovered: number, total: number }
    - getNPCsMet(npcsMetIds: string[]): { discovered: {id,name}[], undiscovered: number, total: number }
    - getItemsFound(inventoryItemIds: string[]): { discovered: {id,name}[], undiscovered: number, total: number }
    - getZoneName(): string

    CREATE src/systems/SaveManager.ts as a pure TypeScript class (NO Phaser imports):
    - SAVE_KEY = 'rcurring-world-save', CURRENT_VERSION = 1
    - save(state: GameState): boolean -- try/catch localStorage.setItem, returns false on error
    - load(): GameState | null -- try/catch localStorage.getItem + JSON.parse, returns null on error/missing
    - hasSave(): boolean -- checks localStorage.getItem !== null
    - deleteSave(): void -- localStorage.removeItem
    - Private migrate(data: any): GameState -- future version migration hook

    CREATE test files following the existing test patterns in tests/unit/ (see zone-manager.test.ts for import/describe/it pattern):

    tests/unit/quest-manager.test.ts:
    - Test accept quest succeeds when no active quest
    - Test accept quest fails when another quest is active
    - Test offerQuest creates entry with 'offered' status
    - Test completeObjective transitions to 'in-progress'
    - Test completeObjective on last objective transitions to 'complete' and clears activeQuestId
    - Test getProgress returns correct counts
    - Test loadState/getState round-trip
    - Test declining quest keeps status 'offered'

    tests/unit/inventory-manager.test.ts:
    - Test addItem succeeds for new item
    - Test addItem rejects duplicate
    - Test addItem rejects when at capacity (12)
    - Test hasItem returns true/false correctly
    - Test loadState/getState round-trip

    tests/unit/journal-manager.test.ts:
    - Test completion percentage with 0, partial, and full discovery
    - Test getPlacesDiscovered returns correct discovered/undiscovered counts
    - Test getNPCsMet returns correct counts
    - Test getItemsFound returns correct counts

    tests/unit/save-manager.test.ts:
    - Test save writes to localStorage
    - Test load reads from localStorage
    - Test load returns null for missing save
    - Test load returns null for corrupt JSON
    - Test hasSave returns true/false
    - Test deleteSave removes key
    - Mock localStorage in tests (global override)
  </action>
  <verify>
    <automated>npx vitest run tests/unit/quest-manager.test.ts tests/unit/inventory-manager.test.ts tests/unit/journal-manager.test.ts tests/unit/save-manager.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/systems/QuestManager.ts contains `export class QuestManager`
    - src/systems/QuestManager.ts does NOT contain `import Phaser` or `import.*from 'phaser'`
    - src/systems/InventoryManager.ts contains `export class InventoryManager`
    - src/systems/InventoryManager.ts contains `private readonly capacity = 12`
    - src/systems/InventoryManager.ts does NOT contain `import Phaser`
    - src/systems/JournalManager.ts contains `export class JournalManager`
    - src/systems/JournalManager.ts contains `getCompletionPercentage`
    - src/systems/JournalManager.ts does NOT contain `import Phaser`
    - src/systems/SaveManager.ts contains `export class SaveManager`
    - src/systems/SaveManager.ts contains `rcurring-world-save`
    - src/systems/SaveManager.ts contains `try` and `catch`
    - src/systems/SaveManager.ts does NOT contain `import Phaser`
    - All 4 test files exit with code 0
    - tests/unit/quest-manager.test.ts contains at least 6 test cases (it() blocks)
    - tests/unit/inventory-manager.test.ts contains at least 4 test cases
    - tests/unit/journal-manager.test.ts contains at least 3 test cases
    - tests/unit/save-manager.test.ts contains at least 5 test cases
  </acceptance_criteria>
  <done>All 4 system managers created as pure TypeScript, all unit tests pass, no Phaser dependency in any manager</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/quest-manager.test.ts tests/unit/inventory-manager.test.ts tests/unit/journal-manager.test.ts tests/unit/save-manager.test.ts --reporter=verbose` -- all tests pass
- `npx vitest run tests/unit/constants.test.ts --reporter=verbose` -- existing constants tests still pass
- `npx tsc --noEmit` -- no type errors
</verification>

<success_criteria>
- 4 pure-logic system managers exported from src/systems/
- GameState interface fully defined with all saveable fields
- 18+ Phase 3 event constants added to EVENTS object
- 8 culturally authentic Bengaluru items in items.json
- 5 world pickup definitions in mg-road-pickups.json
- Full discovery checklist in mg-road-discoveries.json
- All unit tests (18+ test cases) pass
- Zero Phaser imports in any new manager
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-systems/03-01-SUMMARY.md`
</output>
