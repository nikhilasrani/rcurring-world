---
phase: 03-game-systems
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/ui/PauseMenu.ts
  - src/ui/InventoryPanel.ts
  - src/ui/QuestPanel.ts
  - src/ui/JournalPanel.ts
  - src/ui/SavePanel.ts
  - src/ui/SettingsPanel.ts
  - src/ui/QuestHUD.ts
  - src/ui/ItemNotification.ts
autonomous: true
requirements:
  - INVT-02
  - PLAT-04
  - JRNL-01
  - JRNL-02
  - JRNL-03
  - SAVE-01

must_haves:
  truths:
    - "Pause menu opens as a full-screen tabbed overlay with 5 tabs"
    - "Inventory tab shows 2x6 grid of 24x24 item slots with selection highlight"
    - "Quest tab shows active quest name and progress, or 'No active quests'"
    - "Journal tab shows Places/NPCs Met/Items Found with completion percentage"
    - "Save tab has save button, last-saved timestamp, and success/failure feedback"
    - "Settings tab has music/SFX volume sliders and run default toggle"
    - "QuestHUD shows active quest progress in top-right corner"
    - "ItemNotification slides down as a banner for item pickups"
  artifacts:
    - path: "src/ui/PauseMenu.ts"
      provides: "Full-screen tabbed overlay"
      exports: ["PauseMenu"]
      contains: "class PauseMenu"
    - path: "src/ui/InventoryPanel.ts"
      provides: "2x6 item grid with selection"
      exports: ["InventoryPanel"]
    - path: "src/ui/QuestPanel.ts"
      provides: "Quest list and detail"
      exports: ["QuestPanel"]
    - path: "src/ui/JournalPanel.ts"
      provides: "Discovery journal display"
      exports: ["JournalPanel"]
    - path: "src/ui/SavePanel.ts"
      provides: "Save/load controls"
      exports: ["SavePanel"]
    - path: "src/ui/SettingsPanel.ts"
      provides: "Volume sliders and toggle"
      exports: ["SettingsPanel"]
    - path: "src/ui/QuestHUD.ts"
      provides: "Corner quest indicator"
      exports: ["QuestHUD"]
    - path: "src/ui/ItemNotification.ts"
      provides: "Got item popup"
      exports: ["ItemNotification"]
  key_links:
    - from: "src/ui/PauseMenu.ts"
      to: "src/ui/InventoryPanel.ts"
      via: "Panel instantiation on tab switch"
      pattern: "InventoryPanel"
    - from: "src/ui/PauseMenu.ts"
      to: "src/utils/constants.ts"
      via: "PAUSE_MENU_OPEN/CLOSE events"
      pattern: "EVENTS.PAUSE_MENU"
    - from: "src/ui/QuestHUD.ts"
      to: "src/utils/constants.ts"
      via: "Quest progress event listening"
      pattern: "EVENTS.QUEST"
---

<objective>
Build all Phase 3 UI components: PauseMenu with 5 tab panels (Inventory, Quests, Journal, Save, Settings), QuestHUD corner indicator, and ItemNotification popup.

Purpose: Provide the visual interface for all game systems. Players need to view inventory, track quests, read journal, save their game, and adjust settings through a cohesive GBA-style pause menu.
Output: 8 new UI component files in src/ui/ that render in UIScene.
</objective>

<execution_context>
@/home/nikhil/Code/Game/rcurring-world/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikhil/Code/Game/rcurring-world/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-game-systems/03-CONTEXT.md
@.planning/phases/03-game-systems/03-UI-SPEC.md
@.planning/phases/03-game-systems/03-01-SUMMARY.md

<interfaces>
From src/ui/ZoneBanner.ts (pattern to reuse for ItemNotification):
```typescript
export class ZoneBanner {
  constructor(scene: Phaser.Scene) { ... }
  reposition(bounds: { x: number; y: number; width: number; height: number }): void { ... }
  show(zoneName: string): void { ... }
  destroy(): void { ... }
}
```

From src/ui/DialogBox.ts (pattern for Phaser UI rendering):
- BOX_HEIGHT = 72, BOX_MARGIN = 4, TEXT_PADDING = 8
- fontFamily: 'monospace', body 10px, name 12px bold
- Colors: #F8F8F8 bg, #222222 border, #111111 text, #333333 name

From src/utils/types.ts (after Plan 01):
```typescript
export interface InventoryItem { id, name, description, iconKey, source }
export interface QuestState { id, status, objectivesCompleted, objectivesTotal }
export interface GameState { version, timestamp, player, quests, inventory, discovery, settings }
```

From src/systems/ (after Plan 01):
```typescript
// QuestManager: getActiveQuestId(), getQuestState(), getProgress()
// InventoryManager: getItems(), getCount()
// JournalManager: getCompletionPercentage(), getPlacesDiscovered(), getNPCsMet(), getItemsFound()
// SaveManager: save(), load(), hasSave()
```

UI-SPEC Layout:
- Pause menu outer margin: 16px from viewport edge (total: 448x288)
- Tab bar: 16px height, labels 10px bold monospace
- Tabs: QUESTS | INVENTORY | JOURNAL | SAVE | SETTINGS
- Active tab: #E8B830 underline (2px)
- Close: X button top-right 16x16
- Panel area: remaining height below tab bar, 16px inner padding
- Inventory grid: 24x24 slots, 4px gap, 2 rows of 6
- QuestHUD: top-right corner, 8px offset, 8px monospace #E8B830 on #111111 bg at 0.7 alpha
- ItemNotification: ZoneBanner-style slide-in, 32px tall, #111111 at 0.7 alpha, 12px bold #FFFFFF
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PauseMenu framework + InventoryPanel + QuestPanel + QuestHUD</name>
  <files>
    src/ui/PauseMenu.ts,
    src/ui/InventoryPanel.ts,
    src/ui/QuestPanel.ts,
    src/ui/QuestHUD.ts
  </files>
  <read_first>
    src/ui/DialogBox.ts,
    src/ui/ZoneBanner.ts,
    src/ui/TouchControls.ts,
    src/scenes/UIScene.ts,
    src/utils/constants.ts,
    src/utils/types.ts
  </read_first>
  <action>
    CREATE src/ui/PauseMenu.ts:
    - Class PauseMenu with constructor(scene: Phaser.Scene)
    - Properties:
      - private container: Phaser.GameObjects.Container (holds everything, depth 70)
      - private overlay: Phaser.GameObjects.Graphics (dark dim, depth 70)
      - private panelBg: Phaser.GameObjects.Graphics (#F8F8F8 bg with #222222 2px border)
      - private tabTexts: Phaser.GameObjects.Text[] (5 tab labels)
      - private tabUnderline: Phaser.GameObjects.Graphics (#E8B830 2px line under active tab)
      - private closeButton: Phaser.GameObjects.Text ("X" in top-right)
      - private activeTabIndex: number = 0
      - private panels: array of panel instances (set externally or created internally)
      - private isOpen: boolean = false
      - private bounds: viewport bounds

    - Layout per UI-SPEC:
      - Overlay: fillRect full viewport, #111111 at 0.7 alpha
      - Menu container positioned at (16, 16) from viewport top-left
      - Menu size: bounds.width - 32, bounds.height - 32 (448x288 at 480x320)
      - Panel background: fillStyle #F8F8F8 at 0.95, fillRect menu size, lineStyle 2px #222222
      - Tab bar at top: 16px height
      - Tab labels: "QUESTS", "INVENTORY", "JOURNAL", "SAVE", "SETTINGS" in 10px bold monospace #333333
        - Space tabs evenly across the width
      - Active tab underline: 2px #E8B830 line below active tab text
      - Close "X" button: top-right of menu, 16x16, text "X" in 12px bold #333333
        - Make it interactive (setInteractive with hitArea) for touch tap
      - Panel content area: starts at y=16 below tab bar, 16px inner padding

    - Methods:
      - open(): void -- show overlay + container, emit EVENTS.PAUSE_MENU_OPEN, emit EVENTS.MOVEMENT_FREEZE(true), fade in animation (150ms, Power2)
      - close(): void -- hide overlay + container, emit EVENTS.PAUSE_MENU_CLOSE, emit EVENTS.MOVEMENT_FREEZE(false), fade out (150ms)
      - switchTab(index: number): void -- update underline position, show/hide panel content
      - navigateTab(direction: 'left' | 'right'): void -- moves tab index with wrap-around
      - isMenuOpen(): boolean
      - setPanels(quests: QuestPanel, inventory: InventoryPanel, journal: JournalPanel, save: SavePanel, settings: SettingsPanel): void
      - reposition(bounds): void -- recalculate positions on resize
      - destroy(): void

    - Input (handled by parent scene, not PauseMenu itself): PauseMenu exposes methods; UIScene calls them on key events.

    CREATE src/ui/InventoryPanel.ts:
    - Class InventoryPanel with constructor(scene: Phaser.Scene, x: number, y: number, width: number, height: number)
    - Properties:
      - private container: Phaser.GameObjects.Container (depth 71)
      - private slots: Phaser.GameObjects.Graphics[] (12 slot backgrounds)
      - private itemIcons: Phaser.GameObjects.Image[] (item sprite frames)
      - private selectedIndex: number = 0
      - private selectionHighlight: Phaser.GameObjects.Graphics (#E8B830 2px border)
      - private itemNameText: Phaser.GameObjects.Text (12px bold)
      - private itemDescText: Phaser.GameObjects.Text (10px, word-wrapped)
      - private emptyText: Phaser.GameObjects.Text ("No items collected yet.")
      - private emptySubtext: Phaser.GameObjects.Text ("Explore MG Road to find Bengaluru souvenirs!")
      - private headingText: Phaser.GameObjects.Text ("INVENTORY" 12px bold)

    - Grid layout per UI-SPEC:
      - Heading: "INVENTORY" at top, 12px bold, #333333
      - 2 rows x 6 columns of 24x24 slots, 4px gap between
      - Grid centered horizontally in panel area
      - Total grid width: 6 * 24 + 5 * 4 = 164px, height: 2 * 24 + 1 * 4 = 52px
      - Empty slots: #CCCCCC outlined 24x24 squares (lineStyle 1px #CCCCCC)
      - Filled slots: show item icon sprite (16x16) centered in 24x24 slot
      - Selected slot: 2px #E8B830 border around it
      - Below grid (24px gap): item name in 12px bold #333333, flavor text in 10px #111111 word-wrapped

    - Methods:
      - update(items: InventoryItem[]): void -- refresh grid with current inventory
      - navigate(direction: 'up' | 'down' | 'left' | 'right'): void -- move selection in 2D grid
      - show(): void -- set container visible
      - hide(): void -- set container invisible
      - destroy(): void

    CREATE src/ui/QuestPanel.ts:
    - Class QuestPanel with constructor(scene: Phaser.Scene, x: number, y: number, width: number, height: number)
    - Shows active quest or "No active quests" message
    - If quest active: quest name in 12px bold, progress "X/Y objectives" in 10px, objective list
    - If quest complete: quest name + "Complete" in #44AA44
    - Copy per UI-SPEC:
      - Active: "{quest name}" + "{completed}/{total} objectives"
      - None: "No active quests"
      - Complete: "{quest name}" + "Complete" in #44AA44

    - Methods:
      - update(questState: QuestState | null, questName: string): void
      - show(): void
      - hide(): void
      - destroy(): void

    CREATE src/ui/QuestHUD.ts:
    - Class QuestHUD with constructor(scene: Phaser.Scene)
    - Small indicator in top-right corner of viewport showing active quest progress
    - Layout per UI-SPEC:
      - Position: 8px from right edge, 8px from top
      - Background: #111111 at 0.7 alpha, sized to text + 4px horizontal padding, 2px vertical padding
      - Text: "1/3" format in 8px monospace #E8B830
      - Depth: 55 (below dialogue, above banner)
      - Only visible when a quest is active

    - Methods:
      - update(completed: number, total: number): void -- update text, show indicator
      - hide(): void -- hide when no active quest
      - reposition(bounds): void -- recalculate based on viewport
      - destroy(): void
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/PauseMenu.ts contains `export class PauseMenu`
    - src/ui/PauseMenu.ts contains `EVENTS.PAUSE_MENU_OPEN`
    - src/ui/PauseMenu.ts contains `EVENTS.MOVEMENT_FREEZE`
    - src/ui/PauseMenu.ts contains `setDepth(70)`
    - src/ui/PauseMenu.ts contains `#E8B830` (tab underline color)
    - src/ui/PauseMenu.ts contains `0.7` (overlay alpha)
    - src/ui/PauseMenu.ts contains tab labels `QUESTS` and `INVENTORY` and `JOURNAL` and `SAVE` and `SETTINGS`
    - src/ui/InventoryPanel.ts contains `export class InventoryPanel`
    - src/ui/InventoryPanel.ts contains `24` (slot size reference)
    - src/ui/InventoryPanel.ts contains `#CCCCCC` (empty slot color)
    - src/ui/InventoryPanel.ts contains `No items collected yet`
    - src/ui/QuestPanel.ts contains `export class QuestPanel`
    - src/ui/QuestPanel.ts contains `No active quests`
    - src/ui/QuestPanel.ts contains `#44AA44` (complete color)
    - src/ui/QuestHUD.ts contains `export class QuestHUD`
    - src/ui/QuestHUD.ts contains `setDepth(55)`
    - src/ui/QuestHUD.ts contains `#E8B830` (indicator text color)
    - npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>PauseMenu with tab framework, InventoryPanel with 2x6 grid, QuestPanel with quest display, QuestHUD with corner indicator -- all compile without type errors</done>
</task>

<task type="auto">
  <name>Task 2: JournalPanel + SavePanel + SettingsPanel + ItemNotification</name>
  <files>
    src/ui/JournalPanel.ts,
    src/ui/SavePanel.ts,
    src/ui/SettingsPanel.ts,
    src/ui/ItemNotification.ts
  </files>
  <read_first>
    src/ui/PauseMenu.ts,
    src/ui/InventoryPanel.ts,
    src/ui/ZoneBanner.ts,
    src/utils/constants.ts,
    src/utils/types.ts
  </read_first>
  <action>
    CREATE src/ui/JournalPanel.ts:
    - Class JournalPanel with constructor(scene: Phaser.Scene, x: number, y: number, width: number, height: number)
    - Container at depth 71

    - Layout per UI-SPEC:
      - Zone completion header: "MG Road: {N}% explored" in 12px bold #333333
      - Section headers: "Places", "NPCs Met", "Items Found" in 12px bold #333333
      - Discovery counts: "{section}: {discovered}/{total} discovered" in 10px #111111
      - Under each section: list of discovered entries (name in 10px) and "???" for undiscovered in #666666
      - Empty state: "Your journal is empty." + "Start exploring to fill your journal!"
      - Scrolling not needed at this scale (5 places + 6 NPCs + 8 items fits in panel height)

    - Methods:
      - update(data: { completion: number, places: { discovered, undiscovered, total }, npcs: { discovered, undiscovered, total }, items: { discovered, undiscovered, total } }): void
      - show(): void
      - hide(): void
      - destroy(): void

    CREATE src/ui/SavePanel.ts:
    - Class SavePanel with constructor(scene: Phaser.Scene, x: number, y: number, width: number, height: number)
    - Container at depth 71

    - Layout per UI-SPEC:
      - "SAVE GAME" button: centered text in 12px bold, with background rect. setInteractive for touch.
      - Last saved timestamp: "Last saved: {timestamp}" in 10px #666666 below button
      - If no save: "No saved game." in 10px #666666
      - Feedback text area: shows "Game saved!" in #44AA44 or "Save failed. Storage may be full." in #CC4444 for 2 seconds after save attempt

    - Methods:
      - update(lastSaved: number | null): void -- update timestamp display
      - showFeedback(success: boolean): void -- flash success/failure message
      - onSave: callback setter -- called when save button activated
      - show(): void
      - hide(): void
      - destroy(): void

    CREATE src/ui/SettingsPanel.ts:
    - Class SettingsPanel with constructor(scene: Phaser.Scene, x: number, y: number, width: number, height: number)
    - Container at depth 71

    - Layout:
      - "Music" label (12px bold #333333) + visual slider bar (horizontal line with draggable dot)
        - Slider: 120px wide line in #CCCCCC, filled portion in #E8B830, dot 8x8
      - "SFX" label + slider bar (same style)
      - "Run by default" label (12px bold #333333) + "ON"/"OFF" toggle text
        - Toggle: currently selected state in #E8B830, other in #666666
      - Sliders are visual-only for now (Phase 4 wires audio). Store values in settings state.

    - Methods:
      - update(settings: { musicVolume: number, sfxVolume: number, runDefault: boolean }): void
      - navigate(direction: 'up' | 'down'): void -- cycle through 3 settings items
      - adjustValue(direction: 'left' | 'right'): void -- change slider/toggle
      - getSettings(): { musicVolume: number, sfxVolume: number, runDefault: boolean }
      - show(): void
      - hide(): void
      - destroy(): void

    CREATE src/ui/ItemNotification.ts:
    - Class ItemNotification with constructor(scene: Phaser.Scene)
    - Follows ZoneBanner slide-in pattern exactly
    - Container at depth 55, scrollFactor 0

    - Layout per UI-SPEC:
      - Background: 32px tall, full viewport width, #111111 at 0.7 alpha
      - Text: "Got {item name}!" in 12px bold monospace #FFFFFF, centered
      - Animation: slide from y=-32 to y=8, hold 2000ms, slide to y=-32 (Power2 easing, 300ms each)

    - Methods:
      - show(itemName: string): void -- trigger slide-in animation with "Got {itemName}!" text
      - reposition(bounds): void -- adapt to viewport
      - destroy(): void
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/JournalPanel.ts contains `export class JournalPanel`
    - src/ui/JournalPanel.ts contains `MG Road:` (completion header pattern)
    - src/ui/JournalPanel.ts contains `???` (undiscovered placeholder)
    - src/ui/JournalPanel.ts contains `Your journal is empty`
    - src/ui/SavePanel.ts contains `export class SavePanel`
    - src/ui/SavePanel.ts contains `SAVE GAME`
    - src/ui/SavePanel.ts contains `Game saved!`
    - src/ui/SavePanel.ts contains `Save failed`
    - src/ui/SavePanel.ts contains `#44AA44` (success color)
    - src/ui/SavePanel.ts contains `#CC4444` (failure color)
    - src/ui/SettingsPanel.ts contains `export class SettingsPanel`
    - src/ui/SettingsPanel.ts contains `Music`
    - src/ui/SettingsPanel.ts contains `SFX`
    - src/ui/SettingsPanel.ts contains `Run by default`
    - src/ui/ItemNotification.ts contains `export class ItemNotification`
    - src/ui/ItemNotification.ts contains `Got ` (item name notification prefix)
    - src/ui/ItemNotification.ts contains `setDepth(55)`
    - src/ui/ItemNotification.ts contains `2000` (hold duration)
    - npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>JournalPanel with discovery sections and completion %, SavePanel with save button and feedback, SettingsPanel with volume sliders and run toggle, ItemNotification with slide-in popup -- all compile without errors</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- all new UI components compile
- `npx vitest run --reporter=verbose` -- existing tests still pass
</verification>

<success_criteria>
- PauseMenu with 5-tab navigation, dark overlay, GBA-style panel background
- InventoryPanel with 2x6 grid, 24x24 slots, selection highlight, item detail display
- QuestPanel showing active quest or empty state
- JournalPanel with Places/NPCs/Items sections, completion %, ??? placeholders
- SavePanel with save button, timestamp, success/failure feedback
- SettingsPanel with Music/SFX sliders and run toggle (visual, ready for Phase 4)
- QuestHUD corner indicator for active quest progress
- ItemNotification slide-in popup for item pickups
- All components follow UI-SPEC colors, typography, spacing, and depth map
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-systems/03-04-SUMMARY.md`
</output>
