---
phase: 03-game-systems
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/ui/DialogBox.ts
  - src/ui/DialogueController.ts
  - src/systems/NPCManager.ts
  - src/data/npcs/chai-walla.json
  - src/data/npcs/jogger.json
  - src/data/npcs/shopkeeper.json
  - src/data/npcs/park-coffee-vendor.json
  - src/data/quests/best-filter-coffee.json
  - tests/unit/dialogue-box.test.ts
autonomous: true
requirements:
  - QUST-01
  - QUST-03

must_haves:
  truths:
    - "DialogBox can display choice pages with 2 options and a cursor indicator"
    - "Player can select between Accept/Decline using up/down keys or tap"
    - "Choice result is emitted via DIALOGUE_CHOICE event"
    - "NPCManager returns quest-state-appropriate dialogue based on QuestManager state"
    - "Coffee quest JSON defines 3 objectives (3 coffee spots) with reward item"
    - "Chai-walla NPC has offer/active/complete/done dialogue variants"
  artifacts:
    - path: "src/ui/DialogBox.ts"
      provides: "Choice mechanic extension"
      contains: "choiceIndex"
    - path: "src/ui/DialogueController.ts"
      provides: "Choice page logic"
      contains: "isChoicePage"
    - path: "src/data/quests/best-filter-coffee.json"
      provides: "Coffee quest definition"
      contains: "best-filter-coffee"
    - path: "src/data/npcs/chai-walla.json"
      provides: "Quest-state dialogue"
      contains: "questDialogue"
    - path: "src/data/npcs/park-coffee-vendor.json"
      provides: "New NPC for coffee quest"
      contains: "npc-park-coffee-vendor"
  key_links:
    - from: "src/ui/DialogBox.ts"
      to: "src/utils/constants.ts"
      via: "DIALOGUE_CHOICE event emission"
      pattern: "EVENTS.DIALOGUE_CHOICE"
    - from: "src/systems/NPCManager.ts"
      to: "src/systems/QuestManager.ts"
      via: "Quest state check for dialogue selection"
      pattern: "questManager.*getQuestState"
---

<objective>
Extend the dialogue system with a choice mechanic for quest acceptance, add quest-state-aware dialogue to NPCManager, create the coffee quest data, and add new/extended NPC JSON files.

Purpose: Enable players to accept/decline quests through dialogue and provide the complete "Find the Best Filter Coffee" quest data with NPC dialogue variants.
Output: Extended DialogBox with choice pages, NPCManager with quest-state dialogue, coffee quest JSON, and 4 NPC data files with quest dialogue.
</objective>

<execution_context>
@/home/nikhil/Code/Game/rcurring-world/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikhil/Code/Game/rcurring-world/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-game-systems/03-CONTEXT.md
@.planning/phases/03-game-systems/03-RESEARCH.md
@.planning/phases/03-game-systems/03-UI-SPEC.md
@.planning/phases/03-game-systems/03-01-SUMMARY.md

<interfaces>
From src/ui/DialogBox.ts (current):
```typescript
export class DialogBox {
  private controller: DialogueController;
  private isTypingComplete: boolean = true;
  constructor(scene: Phaser.Scene) { ... }
  show(dialogueData: DialogueData): void { ... }
  advance(): void { ... }
  isActive(): boolean { ... }
  reposition(bounds): void { ... }
  destroy(): void { ... }
}
```

From src/ui/DialogueController.ts (current):
```typescript
export class DialogueController {
  constructor(pages: string[], name?: string) { ... }
  getCurrentPage(): string { ... }
  hasMorePages(): boolean { ... }
  isLastPage(): boolean { ... }
  nextPage(): boolean { ... }
  getName(): string { ... }
}
```

From src/systems/NPCManager.ts (current):
```typescript
export class NPCManager {
  constructor(scene, npcDefs: NPCDef[]) { ... }
  getNPCData(npcId: string): NPCDef | undefined { ... }
  stopNPCPatrol(gridEngine, npcId: string): void { ... }
  resumeNPCPatrol(gridEngine, npcId: string): void { ... }
}
```

From src/utils/types.ts (after Plan 01):
```typescript
export interface QuestDef { id, name, description, giverNpcId, objectives, reward }
export interface QuestObjective { id, description, type, targetId }
export type QuestStatus = 'not-started' | 'offered' | 'accepted' | 'in-progress' | 'complete';
```

From 03-UI-SPEC.md (Dialogue Choice Layout):
- Cursor: ">" in #E8B830, positioned 4px left of choice text
- Selected choice text color: #111111
- Unselected choice text color: #666666
- Choice area starts at y-offset 20px below name
- Choices are 14px apart vertically
- Choice pages block until explicit selection
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DialogueController + DialogBox choice mechanic</name>
  <files>
    src/ui/DialogueController.ts,
    src/ui/DialogBox.ts,
    tests/unit/dialogue-box.test.ts
  </files>
  <read_first>
    src/ui/DialogueController.ts,
    src/ui/DialogBox.ts,
    src/utils/constants.ts,
    tests/unit/dialogue-box.test.ts
  </read_first>
  <behavior>
    DialogueController:
    - isChoicePage() returns true when current page index matches choicePage
    - getChoices() returns the choices array
    - setChoiceData(choicePage, choices) stores choice metadata
    - On choice page, hasMorePages returns false (blocks auto-advance)
    - selectChoice(index) returns the selected choice text

    DialogBox:
    - When displaying a choice page, renders 2 choice texts with cursor ">" on selected
    - Up/down input moves choiceIndex between 0 and 1
    - advance() on choice page emits DIALOGUE_CHOICE with selected index and continues
    - Choice cursor uses #E8B830 color
    - Selected choice text uses #111111, unselected uses #666666
  </behavior>
  <action>
    EXTEND src/ui/DialogueController.ts:
    - Add private choicePage: number = -1 (index of which page is the choice page; -1 means no choice)
    - Add private choices: string[] = []
    - Add setChoiceData(choicePage: number, choices: string[]): void
    - Add isChoicePage(): boolean -- returns true when currentPage === choicePage AND choicePage >= 0
    - Add getChoices(): string[] -- returns this.choices
    - Add getChoiceCount(): number
    - Modify hasMorePages(): when on choice page, return false (block normal advance)
    - Constructor remains unchanged (backward compatible)

    EXTEND src/ui/DialogBox.ts:
    - Add private choiceTexts: Phaser.GameObjects.Text[] = []
    - Add private cursorText: Phaser.GameObjects.Text (the ">" indicator)
    - Add private choiceIndex: number = 0
    - Add private isInChoiceMode: boolean = false

    MODIFY show(dialogueData: DialogueData) to accept optional choice fields:
    - Accept extended DialogueData that may have `choicePage?: number` and `choices?: string[]`
    - If choicePage/choices present, call controller.setChoiceData(choicePage, choices)
    - Create the DialogueData interface extension in types.ts if needed, or handle via optional fields

    ADD showChoices() private method:
    - Called when controller.isChoicePage() returns true
    - Hide normal contentText and pageIndicator
    - Create/show 2 choice text objects positioned inside the dialog box
    - Choice 0 at y = nameText.y + 20, Choice 1 at y = nameText.y + 34 (14px apart)
    - Each choice text: fontFamily 'monospace', fontSize '10px'
    - Selected: color '#111111', unselected: color '#666666'
    - Cursor ">" text in '#E8B830' at x = choiceText.x - 8, y = selected choice y
    - Set isInChoiceMode = true

    ADD moveChoiceCursor(direction: 'up' | 'down') public method:
    - Toggles choiceIndex between 0 and 1
    - Updates cursor position and text colors
    - Only works when isInChoiceMode is true

    MODIFY advance():
    - If isInChoiceMode: emit DIALOGUE_CHOICE event via eventsCenter with { choiceIndex, choiceText: choices[choiceIndex] }
    - Then hide choice elements, set isInChoiceMode = false
    - Continue to next page or close dialogue

    MODIFY the page display logic: after typing completes on a page, check if controller.isChoicePage(). If so, call showChoices() instead of showing page indicator.

    ADD isChoiceActive(): boolean public method (returns isInChoiceMode).

    UPDATE tests/unit/dialogue-box.test.ts:
    - Test DialogueController.isChoicePage() returns true on correct page
    - Test DialogueController.isChoicePage() returns false on non-choice pages
    - Test setChoiceData stores choices correctly
    - Test getChoices returns stored choices
    - Test hasMorePages returns false on choice page
  </action>
  <verify>
    <automated>npx vitest run tests/unit/dialogue-box.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/DialogueController.ts contains `isChoicePage()` method
    - src/ui/DialogueController.ts contains `setChoiceData` method
    - src/ui/DialogueController.ts contains `getChoices()` method
    - src/ui/DialogBox.ts contains `choiceIndex`
    - src/ui/DialogBox.ts contains `isInChoiceMode`
    - src/ui/DialogBox.ts contains `moveChoiceCursor`
    - src/ui/DialogBox.ts contains `EVENTS.DIALOGUE_CHOICE`
    - src/ui/DialogBox.ts contains `#E8B830` (cursor color)
    - src/ui/DialogBox.ts contains `#666666` (unselected color)
    - tests/unit/dialogue-box.test.ts contains `isChoicePage`
    - All dialogue-box tests pass
  </acceptance_criteria>
  <done>DialogBox displays choice pages with cursor navigation and emits DIALOGUE_CHOICE events. DialogueController handles choice page logic. Unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: NPCManager quest-state dialogue + quest/NPC data files</name>
  <files>
    src/systems/NPCManager.ts,
    src/data/npcs/chai-walla.json,
    src/data/npcs/jogger.json,
    src/data/npcs/shopkeeper.json,
    src/data/npcs/park-coffee-vendor.json,
    src/data/quests/best-filter-coffee.json
  </files>
  <read_first>
    src/systems/NPCManager.ts,
    src/data/npcs/chai-walla.json,
    src/data/npcs/jogger.json,
    src/data/npcs/shopkeeper.json,
    src/systems/QuestManager.ts,
    src/utils/types.ts
  </read_first>
  <action>
    CREATE src/data/quests/best-filter-coffee.json:
    ```json
    {
      "id": "best-filter-coffee",
      "name": "Find the Best Filter Coffee",
      "description": "Raju challenges you to try filter coffee at three spots on MG Road and decide whose is best.",
      "giverNpcId": "npc-chai-walla",
      "objectives": [
        {
          "id": "taste-chai-walla",
          "description": "Try Raju's filter coffee",
          "type": "interact-npc",
          "targetId": "npc-chai-walla"
        },
        {
          "id": "taste-coffee-shop",
          "description": "Try coffee at the coffee shop",
          "type": "visit-location",
          "targetId": "interior-coffee-counter"
        },
        {
          "id": "taste-park-stall",
          "description": "Try coffee at the park stall",
          "type": "interact-npc",
          "targetId": "npc-park-coffee-vendor"
        }
      ],
      "reward": {
        "itemId": "best-filter-coffee",
        "dialogueUnlock": "done"
      }
    }
    ```

    EXTEND src/data/npcs/chai-walla.json with questDialogue section (keep existing dialogue intact):
    ```json
    {
      "id": "npc-chai-walla",
      "name": "Raju",
      "spriteKey": "npc-chai-walla",
      "position": { "x": 40, "y": 36 },
      "facing": "down",
      "patrolRadius": 2,
      "patrolDelay": 1500,
      "speed": 2,
      "dialogue": {
        "name": "Raju",
        "pages": [
          "Namaskara! Best filter coffee on MG Road, saar! Twenty rupees only.",
          "Fresh decoction, none of that instant-instant business. My grandfather started this stall in 1985.",
          "You want strong or light? Banni, banni -- come, I'll make fresh for you!"
        ]
      },
      "questDialogue": {
        "questId": "best-filter-coffee",
        "offer": {
          "name": "Raju",
          "pages": [
            "You like filter coffee? I tell you what -- go try coffee at three places.",
            "Try mine here, try at the coffee shop inside, and try at Cubbon Park stall.",
            "Then come back and tell me whose is best!"
          ],
          "choicePage": 3,
          "choices": ["Accept: Okay, challenge accepted!", "Decline: Maybe later, Raju."]
        },
        "active": {
          "name": "Raju",
          "pages": [
            "Still trying the coffees? Come back when you've tried all three, saar!"
          ]
        },
        "complete": {
          "name": "Raju",
          "pages": [
            "So? Whose coffee is best?",
            "Haha! I knew it! My grandfather's recipe is unbeatable!",
            "Here, take this -- my special tumbler. Best Filter Coffee, saar!"
          ]
        },
        "done": {
          "name": "Raju",
          "pages": [
            "Ah, my favorite customer! Come back anytime for a fresh cup, saar."
          ]
        }
      }
    }
    ```

    Note on choicePage: The value 3 means "show choices after page index 2 (the 3rd page)". The DialogueController should interpret this as: after displaying pages[0], pages[1], pages[2] normally, the next advance triggers the choice display instead of advancing to a 4th page.

    CREATE src/data/npcs/park-coffee-vendor.json:
    ```json
    {
      "id": "npc-park-coffee-vendor",
      "name": "Park Coffee Vendor",
      "spriteKey": "npc-park-coffee-vendor",
      "position": { "x": 8, "y": 18 },
      "facing": "down",
      "patrolRadius": 1,
      "patrolDelay": 2000,
      "speed": 2,
      "dialogue": {
        "name": "Lakshmi",
        "pages": [
          "Filter coffee! Fresh filter coffee! Best in Cubbon Park!",
          "I've been selling coffee here for fifteen years. Morning walkers love it!"
        ]
      },
      "questDialogue": {
        "questId": "best-filter-coffee",
        "questObjectiveId": "taste-park-stall",
        "active": {
          "name": "Lakshmi",
          "pages": [
            "Oh, Raju sent you? That old rascal thinks his coffee is best!",
            "Here, try mine -- I use special Chikmagalur beans!"
          ]
        },
        "afterObjective": {
          "name": "Lakshmi",
          "pages": [
            "You already tried mine! Go tell Raju the verdict, haha!"
          ]
        }
      }
    }
    ```

    EXTEND src/data/npcs/shopkeeper.json with questDialogue for coffee shop objective:
    Add a questDialogue section with questId "best-filter-coffee", questObjectiveId "taste-coffee-shop" with active and afterObjective variants. The shopkeeper can mention the coffee counter inside the shop.

    EXTEND src/data/npcs/jogger.json: No quest dialogue needed, but the jogger could have a small quip about the coffee quest if interacted. This is optional -- skip if it overcomplicates.

    EXTEND src/systems/NPCManager.ts:
    - Add optional questManager reference: private questManager: QuestManager | null = null
    - Add setQuestManager(qm: QuestManager): void method
    - MODIFY getNPCData(npcId) or add getDialogueForNPC(npcId: string): DialogueData method:
      - Check if NPC JSON has questDialogue section
      - If questDialogue exists and questManager is set:
        - Get quest state from questManager for questDialogue.questId
        - If quest status is 'not-started' or 'offered': return questDialogue.offer (includes choice data)
        - If 'accepted' or 'in-progress': check if this NPC's specific objective is completed
          - If objective completed: return questDialogue.afterObjective or regular dialogue
          - If not completed: return questDialogue.active
        - If 'complete': return questDialogue.complete
        - If quest was completed and player re-talks: return questDialogue.done
      - If no quest state or no questDialogue: return regular NPC dialogue
    - The return value should include choicePage and choices fields when returning offer dialogue
    - Import QuestManager type (it's a pure TS class, safe to import)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/data/quests/best-filter-coffee.json contains `best-filter-coffee` as id
    - src/data/quests/best-filter-coffee.json contains 3 objectives
    - src/data/quests/best-filter-coffee.json contains `reward` with `itemId`
    - src/data/npcs/chai-walla.json contains `questDialogue` section
    - src/data/npcs/chai-walla.json contains `offer`, `active`, `complete`, `done` dialogue keys
    - src/data/npcs/chai-walla.json contains `choicePage` and `choices` in offer
    - src/data/npcs/park-coffee-vendor.json exists with `npc-park-coffee-vendor` id
    - src/data/npcs/park-coffee-vendor.json contains `Lakshmi` as name
    - src/systems/NPCManager.ts contains `setQuestManager` method
    - src/systems/NPCManager.ts contains `questDialogue` reference
    - src/systems/NPCManager.ts contains `getDialogueForNPC` or equivalent quest-state lookup
    - npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>Coffee quest fully defined in JSON, NPC dialogue variants for all quest states, NPCManager selects dialogue based on quest progress</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/dialogue-box.test.ts --reporter=verbose` -- choice mechanic tests pass
- `npx tsc --noEmit` -- no type errors (validates quest JSON, NPC data, and NPCManager integration)
- `npx vitest run --reporter=verbose` -- all existing tests still pass
</verification>

<success_criteria>
- DialogBox supports choice pages with cursor navigation (up/down) and emits DIALOGUE_CHOICE
- NPCManager returns quest-state-appropriate dialogue based on QuestManager state
- Coffee quest JSON with 3 objectives and Best Filter Coffee reward
- Chai-walla NPC has offer/active/complete/done dialogue with choicePage+choices in offer
- Park coffee vendor NPC data created with quest dialogue variants
- All tests pass, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-systems/03-03-SUMMARY.md`
</output>
