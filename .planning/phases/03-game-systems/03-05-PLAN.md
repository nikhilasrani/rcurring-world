---
phase: 03-game-systems
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-04"]
files_modified:
  - src/ui/MetroMap.ts
  - src/entities/ItemPickup.ts
  - src/data/interiors/metro-station.json
  - src/data/interiors/coffee-shop.json
autonomous: true
requirements:
  - MTRO-01
  - MTRO-02
  - MTRO-03
  - INVT-01

must_haves:
  truths:
    - "Metro map overlay displays Namma Metro Purple Line diagram with station dots"
    - "Current station (MG Road) highlighted in #E8B830, others greyed out"
    - "Locked stations show 'Coming in future update' message"
    - "Metro travel has 3-part transition: doors close (0.5s), ride (1.5s), doors open (0.5s)"
    - "ItemPickup entity renders sparkly collectible on world tiles"
    - "Interior JSON files extended with interactables array"
  artifacts:
    - path: "src/ui/MetroMap.ts"
      provides: "Metro map overlay"
      exports: ["MetroMap"]
      contains: "class MetroMap"
    - path: "src/entities/ItemPickup.ts"
      provides: "World sparkle pickup entity"
      exports: ["ItemPickup"]
      contains: "class ItemPickup"
    - path: "src/data/interiors/metro-station.json"
      provides: "Metro station with interactables"
      contains: "interactables"
    - path: "src/data/interiors/coffee-shop.json"
      provides: "Coffee shop with interactables"
      contains: "interactables"
  key_links:
    - from: "src/ui/MetroMap.ts"
      to: "src/utils/constants.ts"
      via: "METRO_MAP_OPEN event"
      pattern: "EVENTS.METRO_MAP_OPEN"
    - from: "src/entities/ItemPickup.ts"
      to: "src/utils/constants.ts"
      via: "TILE_SIZE for positioning"
      pattern: "TILE_SIZE"
---

<objective>
Build the MetroMap overlay UI with Purple Line diagram and travel transition animation, create the ItemPickup world entity for sparkly collectibles, and extend interior JSON data with interactable definitions.

Purpose: Enable metro fast-travel UI (even though destinations are locked in v1, the UI is functional) and world item pickup entities. Interior interactables enable coffee quest and metro map interactions inside buildings.
Output: MetroMap overlay component, ItemPickup entity class, and extended interior data files.
</objective>

<execution_context>
@/home/nikhil/Code/Game/rcurring-world/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikhil/Code/Game/rcurring-world/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-game-systems/03-CONTEXT.md
@.planning/phases/03-game-systems/03-UI-SPEC.md
@.planning/phases/03-game-systems/03-01-SUMMARY.md
@.planning/phases/03-game-systems/03-04-SUMMARY.md

<interfaces>
From src/ui/PauseMenu.ts (Plan 04 pattern for overlays):
- Full-screen overlay at depth 70 with dark dim background
- Container-based rendering with reposition for viewport adaptation

From src/utils/types.ts (after Plan 01):
```typescript
export interface PickupDef {
  id: string;
  itemId: string;
  position: { x: number; y: number };
  zone: string;
}

export interface InteriorInteractable {
  id: string;
  type: 'counter' | 'metro-map' | 'object';
  position: { x: number; y: number };
  dialogue?: DialogueData;
}
```

From src/utils/constants.ts:
```typescript
TILE_SIZE = 16;
EVENTS.METRO_MAP_OPEN = 'metro-map-open';
EVENTS.METRO_TRAVEL_START = 'metro-travel-start';
EVENTS.ITEM_PICKUP_INTERACT = 'item-pickup-interact';
ASSETS.SPRITE_SPARKLE = 'sparkle';
```

From src/data/interiors/metro-station.json (current):
```json
{
  "id": "interior-metro",
  "name": "metro-station",
  "displayName": "Metro Station",
  "tilemapKey": "tilemap-interior-metro",
  "tilesetKey": "tileset-interior",
  "doorPosition": { "x": 44, "y": 33 },
  "playerSpawn": { "x": 7, "y": 6 },
  "exitPosition": { "x": 7, "y": 9 },
  "returnPosition": { "x": 44, "y": 34 },
  "size": { "width": 15, "height": 10 }
}
```

UI-SPEC Metro Map Layout:
- Full-screen overlay (448x288, same as pause menu)
- "Namma Metro - Purple Line" title 12px bold
- Station dots: 6px circles on horizontal line
- Line color: #8B45A6 (Namma Purple Line)
- Current station (MG Road): #E8B830 filled dot
- Locked stations: #666666 hollow dots
- Close: X button top-right
- Locked tooltip: "Coming in future update" in #666666
- Travel transition: doors (0.5s), ride (1.5s), doors (0.5s) using Quad.easeInOut
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MetroMap overlay with travel transition</name>
  <files>src/ui/MetroMap.ts</files>
  <read_first>
    src/ui/PauseMenu.ts,
    src/ui/ZoneBanner.ts,
    src/systems/TransitionManager.ts,
    src/utils/constants.ts,
    src/utils/types.ts
  </read_first>
  <action>
    CREATE src/ui/MetroMap.ts:
    - Class MetroMap with constructor(scene: Phaser.Scene)
    - Properties:
      - private container: Phaser.GameObjects.Container (depth 70)
      - private overlay: Phaser.GameObjects.Graphics (dark dim #111111 at 0.7)
      - private mapBg: Phaser.GameObjects.Graphics (#F8F8F8 bg, #222222 2px border)
      - private titleText: Phaser.GameObjects.Text ("Namma Metro - Purple Line" 12px bold)
      - private closeButton: Phaser.GameObjects.Text ("X" top-right, interactive)
      - private stationDots: Phaser.GameObjects.Graphics[]
      - private stationLabels: Phaser.GameObjects.Text[]
      - private tooltipText: Phaser.GameObjects.Text ("Coming in future update" #666666)
      - private isOpen: boolean = false
      - private bounds: viewport bounds
      - private selectedStationIndex: number = 1 (MG Road = index 1, default)

    - Station data (hardcoded for v1 -- 3 stations):
      ```
      const stations = [
        { id: 'majestic', name: 'Majestic', unlocked: false },
        { id: 'mg-road', name: 'MG Road', unlocked: true },
        { id: 'indiranagar', name: 'Indiranagar', unlocked: false },
      ];
      ```

    - Layout (within 448x288 menu area, 16px margin from viewport edges):
      - Title "Namma Metro - Purple Line" centered at top, 12px bold #333333
      - Close "X" button at top-right, 16x16, interactive
      - Horizontal purple line: 2px stroke #8B45A6, centered vertically
      - Station dots along the line: 6px diameter circles
        - Majestic (left): #666666 hollow circle (strokeCircle)
        - MG Road (center): #E8B830 filled circle (fillCircle)
        - Indiranagar (right): #666666 hollow circle
      - Station name labels: 10px monospace below each dot
        - MG Road label in #E8B830
        - Others in #666666
      - Tooltip "Coming in future update" at bottom center in 10px #666666 (shown when locked station selected)

    - Methods:
      - open(): void -- show overlay + container, emit EVENTS.MOVEMENT_FREEZE(true), fade in 150ms
      - close(): void -- hide, emit EVENTS.MOVEMENT_FREEZE(false), fade out 150ms
      - selectStation(index: number): void -- highlight selected station. If locked, show tooltip.
      - navigateStation(direction: 'left' | 'right'): void -- move selection between stations
      - confirmStation(): void -- if unlocked station selected, trigger travel. If locked, show tooltip text.
      - isMapOpen(): boolean
      - reposition(bounds): void
      - destroy(): void

    - Travel transition (triggered when player confirms travel to unlocked station -- currently only MG Road to MG Road, but the mechanism works):
      - playTravelTransition(destinationName: string): void
        1. Doors close (0.5s): Two black rectangles (#000000) slide from left/right edges toward center using scene.tweens.add with Quad.easeInOut
        2. Ride screen (1.5s): Full black background with "Next stop: {station name}" text in 12px bold #FFFFFF centered. Optional: subtle horizontal scroll of abstract line at bottom.
        3. Doors open (0.5s): Two black rectangles slide from center to edges, revealing game world
        4. After complete: emit EVENTS.METRO_TRAVEL_START with destination data, close metro map
      - Use Phaser tweens for all animations. Create the door rectangles as Graphics objects at depth 80 (above everything).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/MetroMap.ts contains `export class MetroMap`
    - src/ui/MetroMap.ts contains `Namma Metro - Purple Line`
    - src/ui/MetroMap.ts contains `#8B45A6` (purple line color)
    - src/ui/MetroMap.ts contains `#E8B830` (current station highlight)
    - src/ui/MetroMap.ts contains `#666666` (locked station color)
    - src/ui/MetroMap.ts contains `Coming in future update`
    - src/ui/MetroMap.ts contains `setDepth(70)`
    - src/ui/MetroMap.ts contains `playTravelTransition`
    - src/ui/MetroMap.ts contains `Quad.easeInOut` or `Quad` (door animation easing)
    - src/ui/MetroMap.ts contains `500` (door animation duration 0.5s)
    - src/ui/MetroMap.ts contains `1500` (ride screen duration 1.5s)
    - src/ui/MetroMap.ts contains `EVENTS.METRO_MAP_OPEN` or `EVENTS.MOVEMENT_FREEZE`
    - src/ui/MetroMap.ts contains `Next stop:`
    - npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>MetroMap overlay renders Purple Line diagram with 3 stations, current station highlighted, locked stations greyed, travel transition plays doors-close/ride/doors-open animation</done>
</task>

<task type="auto">
  <name>Task 2: ItemPickup entity + interior interactable data</name>
  <files>
    src/entities/ItemPickup.ts,
    src/data/interiors/metro-station.json,
    src/data/interiors/coffee-shop.json
  </files>
  <read_first>
    src/entities/NPC.ts,
    src/data/interiors/metro-station.json,
    src/data/interiors/coffee-shop.json,
    src/utils/constants.ts,
    src/utils/types.ts
  </read_first>
  <action>
    CREATE src/entities/ItemPickup.ts:
    - Class ItemPickup
    - Constructor(scene: Phaser.Scene, pickupId: string, itemId: string, tileX: number, tileY: number, spriteKey: string)
    - Properties:
      - public sprite: Phaser.GameObjects.Sprite
      - public pickupId: string
      - public itemId: string
      - public tilePosition: { x: number; y: number }

    - Sprite positioning: tileX * TILE_SIZE + TILE_SIZE / 2, tileY * TILE_SIZE + TILE_SIZE / 2
    - Sprite depth: 2 (above ground, below player/NPCs)

    - Sparkle animation (bob + alpha pulse):
      ```typescript
      scene.tweens.add({
        targets: this.sprite,
        y: this.sprite.y - 2,
        alpha: { from: 0.7, to: 1 },
        duration: 600,
        yoyo: true,
        repeat: -1,
        ease: 'Sine.easeInOut',
      });
      ```

    - Methods:
      - destroy(): void -- destroys sprite and stops tweens
      - isAtPosition(tileX: number, tileY: number): boolean -- checks if pickup is at given tile

    EXTEND src/data/interiors/metro-station.json -- add `interactables` array:
    ```json
    {
      "id": "interior-metro",
      "name": "metro-station",
      "displayName": "Metro Station",
      "tilemapKey": "tilemap-interior-metro",
      "tilesetKey": "tileset-interior",
      "doorPosition": { "x": 44, "y": 33 },
      "playerSpawn": { "x": 7, "y": 6 },
      "exitPosition": { "x": 7, "y": 9 },
      "returnPosition": { "x": 44, "y": 34 },
      "size": { "width": 15, "height": 10 },
      "interactables": [
        {
          "id": "metro-map-wall",
          "type": "metro-map",
          "position": { "x": 7, "y": 3 }
        }
      ]
    }
    ```
    Position (7,3) is a wall tile near the top of the metro station interior where a map would logically be posted. Adjust if the tilemap suggests a better spot.

    EXTEND src/data/interiors/coffee-shop.json -- add `interactables` array:
    ```json
    {
      "id": "interior-coffee",
      "name": "coffee-shop",
      "displayName": "Raju's Filter Coffee",
      "tilemapKey": "tilemap-interior-coffee",
      "tilesetKey": "tileset-interior",
      "doorPosition": { "x": 40, "y": 36 },
      "playerSpawn": { "x": 5, "y": 8 },
      "exitPosition": { "x": 5, "y": 9 },
      "returnPosition": { "x": 40, "y": 37 },
      "size": { "width": 12, "height": 10 },
      "interactables": [
        {
          "id": "interior-coffee-counter",
          "type": "counter",
          "position": { "x": 6, "y": 4 },
          "dialogue": {
            "name": "Coffee Counter",
            "pages": [
              "A fresh pot of filter coffee is brewing behind the counter.",
              "The aroma of Chikmagalur beans fills the room!"
            ]
          }
        }
      ]
    }
    ```
    The counter interactable at (6,4) is within the interior map bounds. The dialogue is for normal interaction. Quest-state-specific dialogue will be handled by NPCManager/QuestManager integration in Plan 06.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const m=require('./src/data/interiors/metro-station.json'); console.log(m.interactables?'OK':'FAIL')" && node -e "const c=require('./src/data/interiors/coffee-shop.json'); console.log(c.interactables?'OK':'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - src/entities/ItemPickup.ts contains `export class ItemPickup`
    - src/entities/ItemPickup.ts contains `TILE_SIZE`
    - src/entities/ItemPickup.ts contains `setDepth(2)`
    - src/entities/ItemPickup.ts contains `Sine.easeInOut`
    - src/entities/ItemPickup.ts contains `duration: 600`
    - src/entities/ItemPickup.ts contains `alpha: { from: 0.7, to: 1 }` or equivalent
    - src/data/interiors/metro-station.json contains `interactables`
    - src/data/interiors/metro-station.json contains `metro-map-wall`
    - src/data/interiors/metro-station.json contains `"type": "metro-map"`
    - src/data/interiors/coffee-shop.json contains `interactables`
    - src/data/interiors/coffee-shop.json contains `interior-coffee-counter`
    - src/data/interiors/coffee-shop.json contains `"type": "counter"`
    - npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>ItemPickup entity with sparkle animation, metro station and coffee shop interiors extended with interactable definitions</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- all new code compiles
- `npx vitest run --reporter=verbose` -- existing tests still pass
- Interior JSON files parse correctly with interactables arrays
</verification>

<success_criteria>
- MetroMap renders Purple Line with 3 stations (Majestic, MG Road, Indiranagar)
- MG Road highlighted in gold, others greyed with "Coming in future update"
- Travel transition plays 3-part door animation (0.5s + 1.5s + 0.5s)
- ItemPickup entity renders sparkly sprite on world tiles with bob animation
- Metro station interior has metro-map-wall interactable
- Coffee shop interior has coffee counter interactable with dialogue
</success_criteria>

<output>
After completion, create `.planning/phases/03-game-systems/03-05-SUMMARY.md`
</output>
